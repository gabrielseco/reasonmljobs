{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "bs-jest": "^0.2.0",
    "reason-react": "^0.2.4",
    "reason-scripts": "0.6.11"
  },
  "dependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "start:django": "./scripts/run-django.sh 4000",
    "build": "react-scripts build",
		"postbuild": "./scripts/build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepare": "npm link bs-platform"
  }
}
