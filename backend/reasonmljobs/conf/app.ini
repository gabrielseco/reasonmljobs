[uwsgi]
module = reasonmljobs.wsgi:application

chdir = /code
socket = /code/app.sock

master = true
processes = 5

vacuum = true

daemonize=/code/app.log

die-on-term = true

env=DATABASE_NAME=reasonmljobs
env=USERNAME=root
env=PASSWORD=temporal
env=HOST=localhost
env=PORT=4406


